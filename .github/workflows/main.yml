name: CI

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest
    
    steps:
    - name: Run Linting
      run: |
        echo Add other actions to build,
        echo test, and deploy your project.
